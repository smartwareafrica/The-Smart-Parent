<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/receiver_message_profile_image"
        android:layout_width="50dp"
        android:layout_height="40dp"
        android:src="@drawable/profile_image_placeholder" />

    <TextView
        android:id="@+id/receiver_message_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="2dp"
        android:layout_toEndOf="@+id/receiver_message_profile_image"
        android:background="@drawable/receiver_text_message_background"
        android:padding="10dp"
        android:text="@string/receivers_message"
        android:textColor="@android:color/background_light"
        android:textSize="14sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/sender_message_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="2dp"
        android:layout_alignParentTop="@+id/receiver_message_text_view"
        android:background="@drawable/sender_text_message_background"
        android:padding="10dp"
        android:text="@string/senders_message"
        android:textColor="@android:color/background_light"
        android:textSize="14sp"
        android:textStyle="bold" />

</RelativeLayout>